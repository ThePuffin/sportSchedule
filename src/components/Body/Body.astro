---
import Team from './Team/Team.astro';
import { teamsSelectedId } from '../../store/store.js';
import DateSelector from './DateSelector/DateSelector.astro';

let { activeTeams } = Astro.props;
// const teamsSelectedIds = activeTeams
//   .map((team, id) => {
//     if (id < 5) {
//       return team.id;
//     }
//   })
//   .filter((t) => t);
const teamsSelectedIds = ['DAL', 'NYR', 'FLA', 'EDM'];
teamsSelectedId.set(teamsSelectedIds);
---

<DateSelector />
<table>
  <tbody>
    <tr>
      {
        teamsSelectedIds.length &&
          teamsSelectedIds.map((teamSelectedId: string, i: number) => (
            <>
              <td>
                <Team {teamsSelectedIds} {teamSelectedId} {activeTeams} {i} />
              </td>
            </>
          ))
      }
    </tr>
  </tbody>
</table>
