---
export interface team {
  value: string;
  id: string;
  label: string;
  teamLogo: string;
}

import TeamCard from '../Card/Card.tsx';
const { activeTeams, i } = Astro.props;
const team = activeTeams[i]
const props = { team, i };
---

<TeamCard {...props} client:load />

<style></style>
